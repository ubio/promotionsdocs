import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.D6Is-jdM.js";
const __pageData = JSON.parse('{"title":"Reporting Format","description":"","frontmatter":{},"headers":[],"relativePath":"reporting-format.md","filePath":"reporting-format.md"}');
const _sfc_main = { name: "reporting-format.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="reporting-format" tabindex="-1">Reporting Format <a class="header-anchor" href="#reporting-format" aria-label="Permalink to &quot;Reporting Format&quot;">​</a></h1><p>Validation results should be reported in a consistent structure so they can be mapped into files, dashboards, APIs, or internal review queues.</p><h2 id="core-fields" tabindex="-1">Core Fields <a class="header-anchor" href="#core-fields" aria-label="Permalink to &quot;Core Fields&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>validation_result</code></td><td>Top-level result: <code>VALID</code>, <code>INVALID</code>, or <code>COULD NOT VALIDATE</code>.</td></tr><tr><td><code>error_category</code></td><td>Grouped reason for <code>INVALID</code> or <code>COULD NOT VALIDATE</code>. Empty when the validation result is <code>VALID</code>.</td></tr><tr><td><code>error_code</code></td><td>Specific machine-readable reason for the failed or incomplete validation. Empty when the validation result is <code>VALID</code>.</td></tr><tr><td><code>source_id</code></td><td>Identifier from the source file, feed, or request.</td></tr><tr><td><code>validated_at</code></td><td>Timestamp for when the validation result was produced.</td></tr><tr><td><code>notes</code></td><td>Optional human-readable context for review or debugging.</td></tr></tbody></table><h2 id="result-semantics" tabindex="-1">Result Semantics <a class="header-anchor" href="#result-semantics" aria-label="Permalink to &quot;Result Semantics&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Internal Result</th><th>Meaning</th></tr></thead><tbody><tr><td><code>VALID</code></td><td>Validation reached a conclusion and the promotion worked.</td></tr><tr><td><code>INVALID</code></td><td>Validation reached a conclusion and the promotion did not work.</td></tr><tr><td><code>COULD NOT VALIDATE</code></td><td>Validation could not reach a conclusion.</td></tr></tbody></table><h2 id="downstream-mapping" tabindex="-1">Downstream Mapping <a class="header-anchor" href="#downstream-mapping" aria-label="Permalink to &quot;Downstream Mapping&quot;">​</a></h2><p>Downstream exports may map these internal results to different labels or spreadsheet fields.</p><p>When mapping results downstream:</p><ul><li>Preserve the original input structure when one is provided.</li><li>Keep <code>VALID</code>, <code>INVALID</code>, and <code>COULD NOT VALIDATE</code> as the canonical internal results.</li><li>Do not assign an error category or error code to a <code>VALID</code> validation result.</li><li>Include error categories and error codes where they help with review, tuning, or debugging.</li></ul>', 10)
  ])]);
}
const reportingFormat = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  reportingFormat as default
};
